---
summary: "CLI reference for `omniclaw uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `omniclaw uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
omniclaw uninstall
omniclaw uninstall --all --yes
omniclaw uninstall --dry-run
```
